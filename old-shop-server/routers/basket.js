const Router = require('express')
const router = new Router()
const basket = require('../controller/basket-controller')

// router.post('/', basket.create)
// router.post('/getcurt', basket.getAll)
// router.post('/delete', basket.deleteProd)
// router.post('/order', basket.order)

module.exports = router